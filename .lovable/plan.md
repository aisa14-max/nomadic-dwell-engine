Replace `public/scenes/desert-nightfall.html` with the uploaded `Desert Nightfall _Lovable_ (1).html`, then apply the same `.frame` CSS override used previously (`width:100vw; height:100vh; aspect-ratio:auto`) so the new scene fills the hero iframe without black bars. No other files change — the Landing page iframe already points to that path.